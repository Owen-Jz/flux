# npm tries to install @vercel/analytics optional peers (svelte/@sveltejs/kit)
# that are irrelevant to this Next.js app and collide with vitest's vite version.
# Relax strict peer resolution so local and Vercel installs behave identically.
legacy-peer-deps=true
